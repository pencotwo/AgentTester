# === Rust / Tauri build output ===
/src-tauri/target/
/src-tauri/gen/
# build_exe.bat copies the release exe here after building
/agent-tester.exe
# saved test-run output from the "儲存結果" card (generated, not source)
/result/

# Cargo.lock 建議提交(此為應用程式,鎖定相依版本可確保建置一致)。
# 若你偏好不追蹤,取消下行註解 / uncomment to ignore Cargo.lock:
# /src-tauri/Cargo.lock

# Tauri bundler 暫存 (Windows installer 工具)
/src-tauri/WixTools/
*.msi
*.nsis.zip

# === IDE / 編輯器 ===
.vscode/
!.vscode/extensions.json
.idea/
*.iml
*.suo
*.user

# === 作業系統 ===
Thumbs.db
Desktop.ini
.DS_Store

# === 日誌與暫存檔 ===
*.log
*.tmp
*.bak
*~

# === 本機環境 / 機密設定 ===
.env
.env.local
